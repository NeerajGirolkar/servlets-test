apply plugin: 'java'
apply plugin: 'war'
apply plugin: 'eclipse-wtp'

repositories {
    mavenCentral()
}

/* Change the default port for Jetty */
/* httpPort = 8081 */

dependencies {
	compile 'org.slf4j:slf4j-api:1.7.12'
    compile 'junit:junit:4.12'
    compile 'javax.servlet:javax.servlet-api:3.0.1'
}
